# ========================================
# EPS BACKEND CONFIGURATION
# ========================================

# Application
spring.application.name=EPS-Backend
server.port=8080


spring.datasource.url=jdbc:mysql:https://www.phpmyadmin.co/db_structure.php?db=&db=sql8808221ource.username=${MYSQL_USERNAME}
spring.datasource.password=${FKN5rB2Bdd}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Connection Pool (Optimized)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# JWT Configuration
jwt.secret=${JWT_SECRET:EPS_Secure_Secret_Key_2024_Change_In_Production_Very_Long_String}
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500}

# Logging
logging.level.root=INFO
logging.level.com.eps.backend=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n