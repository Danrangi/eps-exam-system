package com.eps.backend.entity;

/**
 * This ENUM represents the 3 supported exam types.
 * Using ENUM prevents wrong values (like "Jmb" or "WASSCE").
 */
public enum ExamType {
    JAMB,
    WAEC,
    NECO
    
}
